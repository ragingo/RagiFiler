<Window x:Class="RagiFiler.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
        xmlns:vc="clr-namespace:RagiFiler.Views.Controls"
        mc:Ignorable="d"
        d:DesignWidth="800"
        d:DesignHeight="600"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        xmlns:control="clr-namespace:RagiFiler.Controls"
        Width="{Binding Path=Width.Value, Mode=TwoWay}"
        Height="{Binding Path=Height.Value, Mode=TwoWay}"
        Title="RagiFiler">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding Path=WindowLoadedCommand}" />
        </i:EventTrigger>
        <i:EventTrigger EventName="Closing">
            <i:InvokeCommandAction Command="{Binding Path=WindowClosingCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <TabControl ItemsSource="{Binding Path=TabItems}">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <TextBlock Text="{Binding Path=Title.Value}" />
                    </Grid>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="300" />
                            <ColumnDefinition Width="500" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3">
                            <Ribbon ShowQuickAccessToolBarOnTop="False">
                                <Ribbon.ApplicationMenu>
                                    <RibbonApplicationMenu>
                                        <RibbonApplicationMenuItem Header="終了" />
                                    </RibbonApplicationMenu>
                                </Ribbon.ApplicationMenu>
                                <RibbonTab Header="表示">
                                    <RibbonGroup Header="表示 / 非表示">
                                        <RibbonCheckBox Label="隠しファイル" IsChecked="{Binding RelativeSource={RelativeSource AncestorType=Window, Mode=FindAncestor}, Path=DataContext.ShowHiddenFiles.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                        <RibbonCheckBox Label="システムァイル" IsChecked="{Binding RelativeSource={RelativeSource AncestorType=Window, Mode=FindAncestor}, Path=DataContext.ShowSystemFiles.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    </RibbonGroup>
                                </RibbonTab>
                                <RibbonTab Header="検索">
                                    <RibbonGroup Header="設定">
                                        <RibbonCheckBox Label="再帰" IsChecked="{Binding RelativeSource={RelativeSource AncestorType=Window, Mode=FindAncestor}, Path=DataContext.RecursiveSearch.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                        <StackPanel Orientation="Horizontal">
                                            <Label>単位</Label>
                                            <ComboBox Width="50" HorizontalContentAlignment="Center">
                                                <ComboBoxItem IsSelected="True">B</ComboBoxItem>
                                                <ComboBoxItem>KB</ComboBoxItem>
                                                <ComboBoxItem>MB</ComboBoxItem>
                                                <ComboBoxItem>GB</ComboBoxItem>
                                                <ComboBoxItem>TB</ComboBoxItem>
                                            </ComboBox>
                                        </StackPanel>
                                    </RibbonGroup>
                                    <RibbonGroup Header="検索">
                                        <StackPanel Orientation="Horizontal">
                                            <Label>ファイル名</Label>
                                            <RibbonTextBox Text="{Binding RelativeSource={RelativeSource AncestorType=Window, Mode=FindAncestor}, Path=DataContext.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextBoxWidth="160" />
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <Label>ファイルサイズ</Label>
                                            <Label>Min</Label>
                                            <control:NumericTextBox Text="{Binding RelativeSource={RelativeSource AncestorType=Window, Mode=FindAncestor}, Path=DataContext.SearchMinSize.Value, StringFormat=#\,#, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="80" HorizontalContentAlignment="Center" />
                                            <Label>Max</Label>
                                            <control:NumericTextBox Text="{Binding RelativeSource={RelativeSource AncestorType=Window, Mode=FindAncestor}, Path=DataContext.SearchMaxSize.Value, StringFormat=#\,#, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="80" HorizontalContentAlignment="Center" />
                                        </StackPanel>
                                    </RibbonGroup>
                                </RibbonTab>
                            </Ribbon>
                        </Grid>
                        <Grid Grid.Row="1" Grid.Column="0">
                            <vc:DirectoryTreeView DataContext="{Binding Path=DirectoryTree}" />
                        </Grid>
                        <GridSplitter Grid.Row="1" Grid.Column="0" Width="2" />
                        <Grid Grid.Row="1" Grid.Column="1">
                            <vc:FileListView DataContext="{Binding Path=FileList}" />
                        </Grid>
                        <GridSplitter Grid.Row="1" Grid.Column="1" Width="2" />
                        <Grid Grid.Row="1" Grid.Column="2">
                            <vc:FilePreview DataContext="{Binding Path=FileList.SelectedItem.Value}" Width="Auto" />
                        </Grid>
                    </Grid>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
    </Grid>
</Window>
